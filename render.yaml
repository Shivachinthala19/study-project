services:
  - type: web
    name: futurepath-ai
    runtime: static
    buildCommand: npm install && npm run build
    publishDir: ./dist
    domains:
      - futurepath.com
      - www.futurepath.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
